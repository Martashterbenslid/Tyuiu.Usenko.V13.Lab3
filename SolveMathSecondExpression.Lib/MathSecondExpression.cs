namespace SolveMathSecondExpression.Lib
{
    public class MathSecondExpression
    {
        public double func2(double x2, double y2, double a2)
        {
            return 7 + (((a2 * x2) / (2 * y2)) / ((3 + a2) / x2)) * y2 - (((2 * x2) + y2 + x2 - (3 * a2)) / (a2 + x2 - (8 * y2)));
        }
    }
}
